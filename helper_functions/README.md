# helper_functions

This package consists of a list of functions, that are used to read/write files (Yaml, CSV).
The functions also include to add, delete or modify functions for Rule updater.

## Installation
This package can be installed by the following command

```
pip install -e .
```